import React from "react";
import ReactDOM from "react-dom/client";
import VirtualScrollExample from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VirtualScrollExample />
  </React.StrictMode>
);
